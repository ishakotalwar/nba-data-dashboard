# Mirror of the root requirements.txt, kept here because Vercel's Python
# builder has been observed installing only the root file. Keep the two in
# sync. Contents are kept to what `backend/` actually imports:
#
#   fastapi, pydantic  - the app and its request models
#   pandas, numpy      - all of the analytics
#   fastparquet        - pandas.read_parquet engine (10 MB; pyarrow is 112 MB
#                        and the function bundle is capped at 250 MB unzipped)
#   tenacity           - retry decorators in backend/live.py
#
# For the Streamlit prototype and the ETL, install requirements-local.txt
# instead — it pulls this file in and adds the heavier extras.
fastapi
pydantic
pandas
numpy
fastparquet
tenacity
